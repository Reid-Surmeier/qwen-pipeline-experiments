import {chromium} from '/home/reidsurmeier/orca/workspaces/Qwen Image pipeline/stargazer/node_modules/playwright/index.mjs';
import fs from 'node:fs/promises';
import path from 'node:path';
import assert from 'node:assert/strict';
const out=path.join(import.meta.dirname,'evidence/play');
const browser=await chromium.launch({headless:true,args:['--no-sandbox','--enable-webgl','--use-gl=angle','--use-angle=swiftshader','--enable-unsafe-swiftshader']});
const page=await browser.newPage({viewport:{width:1756,height:1456}});const errors=[];
page.on('pageerror',e=>errors.push(String(e)));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});
await page.goto('https://windows-wsl.taile06c45.ts.net/pixel-atlas-prototype-01a07820/',{waitUntil:'networkidle'});
await page.waitForFunction(()=>window.atlasState&&window.atlasWindow,{timeout:90000});await page.waitForTimeout(1500);
const shot=async name=>{await page.screenshot({path:path.join(out,name+'.png')});await fs.writeFile(path.join(out,name+'.json'),JSON.stringify(await page.evaluate(()=>({window:window.atlasWindow,viewport:window.atlasState.viewport})),null,2));};
await shot('frame-reference-size');
let w=await page.evaluate(()=>window.atlasWindow);assert.deepEqual(w.size,[1724,1424]);assert.deepEqual(w.map_rect,[52,110,1652,1288]);
await page.mouse.move(1736,1436);await page.mouse.down();await page.mouse.move(1112,812,{steps:15});await page.mouse.up();await page.waitForTimeout(600);await shot('frame-reference-resized');
w=await page.evaluate(()=>window.atlasWindow);assert.deepEqual(w.size,[1100,800]);
await page.mouse.move(196,76);await page.mouse.down();await page.mouse.move(296,146,{steps:10});await page.mouse.up();await page.waitForTimeout(500);await shot('frame-reference-moved');
w=await page.evaluate(()=>window.atlasWindow);assert.deepEqual(w.position,[116,86]);
await page.mouse.move(1212,882);await page.mouse.down();await page.mouse.move(300,882,{steps:10});await page.mouse.up();await page.waitForTimeout(500);await shot('frame-reference-minimum');
assert.equal((await page.evaluate(()=>window.atlasState)).viewport[0],300);
assert.equal(errors.length,0);await fs.writeFile(path.join(out,'frame-check.json'),JSON.stringify({status:'pass',checks:['reference-size frame','resize','drag'],errors},null,2));await browser.close();
console.log('PASS reference frame input checks');
